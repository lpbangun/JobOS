import fs from 'node:fs';
import path from 'node:path';
import http from 'node:http';
import { esc } from './utils.js';
import { state } from './analytics.js';
import { handleApi } from './api.js';
function html(st){ const profiles=st.profiles.map(p=>`<article class="card"><h3>${esc(p.name)}</h3><p><code>${esc(p.id)}</code></p></article>`).join('')||'<p class="muted">No profiles yet.</p>'; const jobs=st.jobs.map(x=>`<tr><td><strong>${esc(x.title)}</strong><br><span class="muted">${esc(x.id)}</span></td><td>${esc(x.company)}</td><td>${esc(x.fit_score??'—')}</td><td>${esc(x.application_status||'not tracked')}</td></tr>`).join('')||'<tr><td colspan="4" class="muted">No jobs imported.</td></tr>'; const tasks=st.tasks.map(t=>`<li>${esc(t.title)} — ${esc(t.priority)} — ${esc(t.due_at||'no due date')}</li>`).join('')||'<li class="muted">No open tasks.</li>'; return `<!doctype html><html><head><meta charset="utf-8"><title>JobOS Local Dashboard</title><style>body{font-family:system-ui;margin:0;background:#0f172a;color:#e2e8f0}.app{display:grid;grid-template-columns:220px 1fr;min-height:100vh}.rail{background:#020617;padding:18px}.rail a{display:block;color:#cbd5e1;padding:8px}main{padding:24px}section{background:#111827;border:1px solid #334155;border-radius:16px;padding:18px;margin:14px 0}.card{background:#0b1220;padding:12px;border-radius:10px}.muted{color:#94a3b8}table{width:100%;border-collapse:collapse}td,th{border-bottom:1px solid #334155;padding:8px;text-align:left}.banner{background:#422006}</style></head><body><div class="app"><nav class="rail"><h2>JobOS</h2><a href="#today">Today</a><a href="#jobs">Jobs</a><a href="#applications">Applications</a><a href="#artifacts">Artifacts</a><a href="#research">Research</a><a href="#outreach">Outreach</a><a href="#tasks">Tasks</a><a href="#profile-proof">Profile & Proof</a><a href="#automations">Automations</a><a href="#audit">Audit Log</a></nav><main><section class="banner"><strong>Human gate:</strong> JobOS cannot submit applications, send outreach, or touch external accounts.</section><section id="today"><h1>Today / Priority Brief</h1><p>Command palette: use CLI/API for agent actions.</p></section><section id="jobs"><h1>Jobs</h1><table><tr><th>Role</th><th>Company</th><th>Score</th><th>Application</th></tr>${jobs}</table></section><section id="applications"><h1>Applications</h1><p>${st.applications.length} tracked applications.</p></section><section id="artifacts"><h1>Artifacts</h1><p>${st.artifacts.length} draft artifacts.</p></section><section id="research"><h1>Research</h1><p>Company and stakeholder modules are scaffolded for Sprint 3.</p></section><section id="outreach"><h1>Outreach</h1><p>No outreach is sent from JobOS.</p></section><section id="tasks"><h1>Tasks</h1><ul>${tasks}</ul></section><section id="profile-proof"><h1>Profile & Proof</h1>${profiles}</section><section id="automations"><h1>Automations</h1><p>Weekly review runs locally only.</p></section><section id="audit"><h1>Audit Log</h1><p>${st.audit.length} recent events.</p></section></main></div></body></html>`; }
export function web(s,{port=4317,host='127.0.0.1',onReady=null}={}){ const server=http.createServer(async (req,res)=>{ const raw=req.url||'/'; if(raw.startsWith('/workspace/') && /(\.\.|%2e%2e)/i.test(raw)){res.writeHead(400);res.end('Bad workspace path');return;} const u=new URL(raw,`http://${req.headers.host}`); const api=await handleApi(s,req,res,u); if(api!==false) return; if(u.pathname.startsWith('/workspace/')){ const rel=u.pathname.replace(/^\/workspace\//,''), abs=path.resolve(s.p.ws,rel); if(!abs.startsWith(s.p.ws)||!fs.existsSync(abs)||fs.statSync(abs).isDirectory()){res.writeHead(404);res.end('Not found');return;} res.writeHead(200,{'content-type':'text/plain; charset=utf-8'}); res.end(fs.readFileSync(abs)); return;} if(u.pathname!=='/'){res.writeHead(404);res.end('Not found');return;} res.writeHead(200,{'content-type':'text/html; charset=utf-8'}); res.end(html(state(s))); }); server.listen(port,host,()=>onReady?.()); return server; }
