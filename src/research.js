import path from 'node:path';
import { one, audit, save } from './db.js';
import { writeMd } from './workspace.js';
export function research(s,jid,type){ const job=one(s,'SELECT * FROM jobs WHERE id=?',[jid]); if(!job) throw Error(`Unknown job: ${jid}`); const sourceUrl=String(job.url||'').startsWith('jobos:text:')?'':job.url; const rel=type==='company'?path.join('jobs',jid,'company-dossier.md'):path.join('jobs',jid,'stakeholders.md'); const content= type==='company' ? `# Company research worksheet — ${job.company}\n\n**Related job:** ${job.title} (${job.id})\n**Source URL:** ${sourceUrl || 'not provided'}\n\n## Known from imported job text\n- Company: ${job.company}\n- Location: ${job.location || 'not specified'}\n\n## Research adapter status\nSprint 1 only created the module/API foundation. Sprint 3 must replace this worksheet with web search + LLM facts and source URLs.\n\n## Human gate\nThis command did not browse private accounts, scrape LinkedIn, or fabricate company facts.\n` : `# Stakeholder research worksheet — ${job.title}\n\n## Appropriate contacts to identify\n- Recruiter for this opening\n- Likely hiring manager\n- Team member or alumni connection\n\n## Suppression and relevance policy\n- Draft outreach only after relevance is documented.\n- Do not send messages from JobOS.\n- Pause outreach if application stage changes to interview/offer/rejected unless user reviews.\n\n## Candidates\nNo people were inferred or fabricated. Sprint 3 must add verified public-source stakeholder identification.\n`; writeMd(path.join(s.p.ws,rel),content); audit(s,`research.${type}.created`,'job',jid,{jobId:jid,path:rel}); save(s); return {jobId:jid,path:rel,note:`${type} worksheet created; no external side effects.`}; }
